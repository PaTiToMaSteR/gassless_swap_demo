# paymaster/TODO

- [x] Lock dev framework: **Foundry** (`forge` + `anvil`)
- [x] EntryPoint version locked: **v0.7** (Fuji)
- [x] Decide demo token pair: **stable → WAVAX**
- [x] Implement demo DEX router/pool with on-chain quote function (`DemoPool`, `DemoRouter`)
- [x] Implement Smart Account + Factory with `executeBatch()` (AA `SimpleAccount` sample)
- [x] Implement Paymaster validation rules (shape, allowlists, expiry, slippage, fee>=gas)
- [x] Emit events required for admin analytics (`PostOpHandled`)
- [x] Export minimal ABIs alongside `deployments/*/addresses.json` (`scripts/export-abis.sh`)
