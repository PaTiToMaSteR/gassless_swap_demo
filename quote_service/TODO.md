# quote_service/TODO

- [x] Lock quote API JSON shape (request/response)
- [x] Decide TTL defaults and per-pair config
- [x] Define routing model (single pool vs multi-pool)
- [x] Define how calldata is produced (router ABI encode)
- [x] Add structured logs + optional push to `paymaster_monitor/server`
